<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-R">
  <title>Conceptos de la Cursada</title>
  <style>
    body { font-family: sans-serif; margin: 20px; }
    .card { border: 1px solid #ccc; padding: 10px; margin-bottom: 10px; border-radius: 5px; }
    h3 { color: #333; }
  </style>
</head>
<body>
  <h1>Conceptos de la Cursada (Renderizado con EJS)</h1>

  <% if (conceptos.length > 0) { %>
    <% conceptos.forEach(concepto => { %>
      <div class="card">
        <h3><%= concepto.nombre %></h3>
        <p><%= concepto.descripcion %></p>
      </div>
    <% }) %>
  <% } else { %>
    <p>No hay conceptos para mostrar.</p>
  <% } %>

</body>
</html>